import { Swagger } from '@nestjsx/crud/lib/crud';

// export function getQueryParamsMeta() {
//   const queryParamsMeta = Swagger.createQueryParamsMeta("getManyBase", { description: 'aaa' } );

//   const params = ['filter', 'sort', 'limit', 'offset'];

//   return queryParamsMeta.filter((item) => {
//     return params.includes(item.name);
//   });
// }
